var App = {
  initialize: function() {
    Reviewer.render();
    Review.render();

    // Fetch reviews
  }

  // fetch: function(callback = () => {}) {
  //   Parse.readAll(data => {
  //     // examine the response from the server request:

  //     callback(JSON.parse(data));
  //   });
  // }
};
